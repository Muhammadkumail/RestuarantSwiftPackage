# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RestaurantsApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RestaurantsApp
   pod 'Alamofire' , '5.0.2'
  pod 'SDWebImage' 
end

target 'RestaurantsAppTests' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RestaurantsApp
   pod 'Alamofire' , '5.0.2'
  pod 'SDWebImage'
end
