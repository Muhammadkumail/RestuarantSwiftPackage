import XCTest
@testable import RestaurantPackage

final class RestaurantPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RestaurantPackage().text, "Hello, World!")
    }
}
